#include <stdio.h>
int main()
{
    int x,y,sum,sub,mul,div;
    x=6;
    y=3;
    sum=x+y;
    sub=x-y;
    mul=x*y;
    div=x/y;
    printf("sum is %d\n diff is %d\n product is %d\n quit%d\n ", sum,sub,mul,div);
    return 0;
}